#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @num: given integer
 * Return: Absolute value
 */
int _abs(int num)
{
	if (num < 0)
	{
	return (-num);
	}
	else
	{
	return (num);
	}
}
